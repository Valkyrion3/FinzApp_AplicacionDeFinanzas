# ============================================================================
# CONFIGURACIÓN DE BASE DE DATOS POSTGRESQL EN LA NUBE
# ============================================================================
# 
# PASO 1: Crea una base de datos PostgreSQL en Render.com (gratis)
#         https://render.com → New+ → PostgreSQL
#
# PASO 2: Copia el "External Database URL" que te da Render
#         Se ve algo así: postgresql://user:password@host/database
#
# PASO 3: Crea un archivo .env (copia este archivo) y pega tu URL
#
# ⚠️ NUNCA subas el archivo .env al repositorio (ya está en .gitignore)
# ============================================================================

# URL de conexión a PostgreSQL (Render, Supabase, Railway, Neon, etc.)
DATABASE_URL=postgresql://usuario:contraseña@host:5432/nombre_db

# Entorno (development o production)
# En production se activa SSL automáticamente
NODE_ENV=development

# Puerto donde correrá el API (default: 4000)
PORT=4000

# ============================================================================
# EJEMPLO CON RENDER:
# DATABASE_URL=postgresql://finzapp_user:TuPasswordAqui@dpg-xxx.oregon-postgres.render.com/finzapp
# NODE_ENV=production
# PORT=4000
# ============================================================================
